<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Para acelerómetro (ya está incluido por defecto, pero es bueno explicitarlo) -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DelUVery"
        android:usesCleartextTraffic="true">

        <!-- Activity de Login (pantalla de inicio) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.DelUVery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de Registro -->
        <activity
            android:name=".activities.SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.DelUVery" />

        <!-- Activity principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Activity de perfil -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false"
            android:label="Perfil" />

        <!-- Activity de carrito -->
        <activity
            android:name=".activities.CarritoActivity"
            android:exported="false"
            android:label="Carrito" />

        <!-- Activity de locales -->
        <activity
            android:name=".activities.LocalesActivity"
            android:exported="false"
            android:label="Locales"
            android:parentActivityName=".MainActivity" />

        <!-- Activity de menú/artículos -->
        <activity
            android:name=".activities.MenuActivity"
            android:exported="false"
            android:label="Menú"
            android:parentActivityName=".activities.LocalesActivity" />

        <!-- Activity de pedidos del cliente -->
        <activity
            android:name=".activities.PedidosActivity"
            android:exported="false"
            android:label="Mis Pedidos"
            android:parentActivityName=".MainActivity" />

        <!-- Activity de repartidores -->
        <activity
            android:name=".activities.RepartidoresActivity"
            android:exported="false"
            android:label="Repartidores"
            android:parentActivityName=".MainActivity" />

        <!-- Activity de tracking de entrega -->
        <activity
            android:name=".activities.TrackingActivity"
            android:exported="false"
            android:label="Tracking de Entrega"
            android:parentActivityName=".activities.PedidosActivity" />

    </application>

</manifest>